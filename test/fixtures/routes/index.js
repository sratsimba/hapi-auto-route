'use strict';

module.exports = [
    {
        method: 'GET',
        path: '/',
        handler: (request, h) => {

            return '/';
        }
    },
    {
        method: 'POST',
        path: '/',
        handler: (request, h) => {

            return '/';
        }
    }
];
